"""Utility functions for demo playground."""


def greet(name: str) -> str:
    """Return a personalized greeting.

    Args:
        name: The name to greet

    Returns:
        A greeting string in the format "Hello, {name}!"
    """
    return f"Hello, {name}!"
